# Patrimoine Desktop — Runtime dependencies
# Install: pip install -r requirements.txt

# ── UI ───────────────────────────────────────────────────────────────────────
PyQt6>=6.7
PyQt6-WebEngine>=6.7

# ── Data & computation ───────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.26
scipy>=1.12

# ── Visualization ────────────────────────────────────────────────────────────
plotly>=5.20
matplotlib>=3.8

# ── Finance & market data ────────────────────────────────────────────────────
yfinance>=0.2

# ── Export ───────────────────────────────────────────────────────────────────
fpdf2>=2.7

# ── HTTP ─────────────────────────────────────────────────────────────────────
requests>=2.31

# ── Images ───────────────────────────────────────────────────────────────────
pillow>=10.0

# ── Optional: Turso / libsql remote replica ──────────────────────────────────
# Uncomment to enable encrypted remote database sync.
# libsql>=0.1
# libsql-client>=0.3

# ── Optional: Trade Republic import via pytr ─────────────────────────────────
# Requires a Trade Republic account and 2FA setup.
# Uncomment to enable TR import.
# pytr>=0.4
# curl_cffi>=0.6
# websockets>=15.0
